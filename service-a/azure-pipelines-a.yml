# Build numbering format
name: $(BuildID)

trigger:
  branches:
    include:
      - master
      - feat/*
  paths:
    include:
      - 'service-a/*'

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo "Hello from 'service-a' this is a test"

- script: npm install
  workingDirectory: $(working-dir)

- script: npm run chalk
  workingDirectory: $(working-dir)